# KPOOL

KPOP album pooling mini program and responsive web app.

## Builds

- `npm run build:weapp` — WeChat mini-program output in `dist/`
- `npm run build:h5` — responsive web output in `dist-h5/`

The GitHub Pages workflow automatically builds and publishes the web version
from the `main` branch.
